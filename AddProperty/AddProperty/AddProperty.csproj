<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- No package references: we stopped automating a browser (Playwright) after Idealista's
       bot-detection blocked it. Everything now runs off a browser extension you install
       manually and JSON/HTTP that the .NET base class library already provides. -->

</Project>
